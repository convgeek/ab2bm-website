import { test } from '@playwright/test'

// Stub tests for INFRA-03 (Sanity Studio accessibility)
// These will be filled in once a real Sanity project is provisioned

test.describe('sanity studio', () => {
  test('Sanity Studio loads at /studio without authentication error', async ({ page }) => {
    test.todo()
  })
  test('all document types are visible in Studio sidebar', async ({ page }) => {
    test.todo()
  })
})
