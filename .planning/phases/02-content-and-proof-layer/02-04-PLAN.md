---
phase: 02-content-and-proof-layer
plan: 04
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/app/(site)/audience/page.tsx
  - src/components/sections/PersonaCard.tsx
autonomous: true
requirements:
  - AUDN-01
  - AUDN-02
  - AUDN-03
  - AUDN-04

must_haves:
  truths:
    - "A visitor landing on /audience can read about MSP and MSSP as explicitly named, distinct audience segments"
    - "Four persona cards render (IT Practitioner, IT Decision-Maker/Exec, MSP, MSSP) each with job titles and company profile"
    - "An audience statistics section shows quantified claims with a methodology note"
    - "Industry/vertical breakdown and company size distribution sections are visible"
    - "All sections render gracefully when Sanity has no content yet (fallback state, not crash)"
  artifacts:
    - path: "src/components/sections/PersonaCard.tsx"
      provides: "Reusable persona card component with data-testid=persona-card"
      exports: ["PersonaCard"]
    - path: "src/app/(site)/audience/page.tsx"
      provides: "Audience page fetching audiencePage singleton from Sanity, rendering all four AUDN requirement sections"
      exports: []
  key_links:
    - from: "src/app/(site)/audience/page.tsx"
      to: "src/sanity/lib/queries.ts"
      via: "import { AUDIENCE_PAGE_QUERY }"
      pattern: "AUDIENCE_PAGE_QUERY"
    - from: "src/app/(site)/audience/page.tsx"
      to: "src/components/sections/PersonaCard.tsx"
      via: "import { PersonaCard } and map over page?.personas"
      pattern: "PersonaCard"
---

<objective>
Build the Audience page at /audience — the key differentiator page that names MSPs and MSSPs as distinct segments and presents verified audience stats. This directly addresses the core value proposition: tech vendor marketers must immediately understand who Ab2bm reaches.

Purpose: Satisfy AUDN-01 through AUDN-04. The page is data-driven via Sanity's audiencePage singleton (no hard-coded stats). Content is published after Ab2bm delivers verified numbers — the page renders an empty/placeholder state until then.
Output: PersonaCard component + audience page route.
</objective>

<execution_context>
@/Users/NickCavalancia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/NickCavalancia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-content-and-proof-layer/02-RESEARCH.md
@src/sanity/lib/queries.ts
@src/sanity/lib/live.ts
@src/app/(site)/page.tsx
@src/components/sections/AudienceStats.tsx

<interfaces>
From src/sanity/lib/queries.ts (added in 02-02):
```typescript
export const AUDIENCE_PAGE_QUERY
// Returns: {
//   pageHeadline, pageSubheadline, methodologyNote,
//   totalAudienceStats: [{ label, value, footnote }],
//   industryBreakdown: [{ vertical, percentage }],
//   companySizeDistribution: [{ tier, percentage }],
//   personas: [{ segmentName, description, jobTitles: string[], companyProfile }]
// }
```

CRITICAL: All stats MUST come from Sanity data, never hard-coded. Component falls back to null/empty, not to placeholder numbers (RESEARCH.md Pitfall 5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build PersonaCard reusable section component</name>
  <files>src/components/sections/PersonaCard.tsx</files>
  <action>
Create `src/components/sections/PersonaCard.tsx` — a reusable card component for audience persona segments.

Props interface:
```typescript
interface PersonaCardProps {
  segmentName: string
  description: string
  jobTitles: string[]
  companyProfile: string
}
```

Rendering:
- Outer element: `<div data-testid="persona-card" className="rounded-xl border border-border bg-card p-6 flex flex-col gap-3">`
- Segment name: `<h3 className="text-xl font-semibold">{segmentName}</h3>`
- Description: `<p className="text-muted-foreground text-sm">{description}</p>`
- Job titles: rendered as a `<ul>` with list items, label "Typical Titles:" above the list
- Company profile: `<p className="text-sm mt-2">{companyProfile}</p>` with label "Company Profile:" above it

Export as named export `PersonaCard`. This is a pure presentational component — no data fetching, no client-side state.
  </action>
  <verify>
    <automated>cd /Users/NickCavalancia/Documents/GitHub/ab2bm-website && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>PersonaCard.tsx exists, exports PersonaCard, has data-testid="persona-card" on root element. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Build audience page at /audience</name>
  <files>src/app/(site)/audience/page.tsx</files>
  <action>
Create `src/app/(site)/audience/page.tsx`. Server Component that fetches the `audiencePage` singleton and renders four distinct sections.

**Data fetching:** Wrap `sanityFetch({ query: AUDIENCE_PAGE_QUERY })` in try/catch with null fallback — identical to homepage.tsx and about page patterns.

**Section 1 — Hero/intro:** Page headline and subheadline from Sanity data. Fallback: `"Our Audience"` / `"The IT professionals and channel partners Ab2bm reaches"`.

**Section 2 — Persona cards** (AUDN-01, AUDN-02): Map over `page?.personas ?? []` rendering `<PersonaCard>` for each. Personas represent IT Practitioner, IT Decision-Maker/Exec, MSP, and MSSP — the four segments. CRITICAL: MSPs and MSSPs must be listed as distinct segments (not grouped). When no personas are in Sanity yet, render a placeholder message "Audience segment profiles coming soon." Do NOT hard-code persona details.

**Section 3 — Audience stats + methodology note** (AUDN-03):
- Stats grid: `data-testid="audience-stats"` on the wrapping element, map over `page?.totalAudienceStats ?? []` rendering stat items (label + value + optional footnote)
- Methodology note: `data-testid="methodology-note"` on the element, renders `page?.methodologyNote`
- When empty: render the stats wrapper with data-testid but show "Verified audience data coming soon" message inside

**Section 4 — Industry breakdown + company size distribution** (AUDN-04):
- Industry breakdown table/list: `data-testid="industry-breakdown"`, map over `page?.industryBreakdown ?? []`
- Company size distribution: `data-testid="company-size-distribution"`, map over `page?.companySizeDistribution ?? []`
- Both render as simple labeled lists or two-column layouts
- When arrays are empty: render wrapper with data-testid but "Data coming soon" message

**Layout:** Max width `max-w-6xl mx-auto`, consistent padding `px-4 sm:px-6 lg:px-8`. Sections separated by `py-16`. Persona cards in responsive grid `grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-4` (4 columns for 4 segments on large screens).
  </action>
  <verify>
    <automated>cd /Users/NickCavalancia/Documents/GitHub/ab2bm-website && npm run build 2>&1 | tail -15</automated>
  </verify>
  <done>/audience route builds. All four data-testid attributes (persona-card, audience-stats, methodology-note, industry-breakdown, company-size-distribution) are present in the rendered output. Empty/fallback state renders without errors. Build passes.</done>
</task>

</tasks>

<verification>
`npm run build` passes. `/audience` appears in build output. Run `npx playwright test tests/e2e/audience.spec.ts --reporter=list` — the "page loads at /audience" test passes; data-testid tests pass with empty-state content; count/content tests remain failing until Sanity content is published (expected).
</verification>

<success_criteria>
- PersonaCard.tsx exists with correct props interface and data-testid="persona-card"
- /audience page exists with all four data-testid attributes present
- MSP and MSSP are explicitly named (in fallback text or Sanity-driven content)
- All sections degrade gracefully when Sanity has no audiencePage document
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-and-proof-layer/02-04-SUMMARY.md`
</output>
