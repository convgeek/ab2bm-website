---
phase: 02-content-and-proof-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/audience.spec.ts
  - tests/e2e/blog.spec.ts
  - tests/e2e/case-studies.spec.ts
  - tests/e2e/homepage.spec.ts
autonomous: true
requirements:
  - AUDN-01
  - AUDN-02
  - AUDN-03
  - AUDN-04
  - BLOG-01
  - BLOG-02
  - BLOG-03
  - CASE-01
  - CASE-02
  - CASE-03
  - TRST-01
  - TRST-03

must_haves:
  truths:
    - "Every Phase 2 requirement has an automated e2e test that can run"
    - "Tests for audience page, blog, and case studies exist as runnable (failing) specs"
    - "Homepage test.todo() placeholders for social proof and logo strip are implemented as real tests"
  artifacts:
    - path: "tests/e2e/audience.spec.ts"
      provides: "E2e stubs for AUDN-01/02/03/04"
      exports: []
    - path: "tests/e2e/blog.spec.ts"
      provides: "E2e stubs for BLOG-01/02/03"
      exports: []
    - path: "tests/e2e/case-studies.spec.ts"
      provides: "E2e stubs for CASE-01/02/03"
      exports: []
    - path: "tests/e2e/homepage.spec.ts"
      provides: "Implemented tests replacing test.todo() for TRST-01/03/CASE-03"
      exports: []
  key_links:
    - from: "tests/e2e/audience.spec.ts"
      to: "/audience"
      via: "page.goto('/audience')"
      pattern: "goto.*audience"
    - from: "tests/e2e/blog.spec.ts"
      to: "/blog"
      via: "page.goto('/blog')"
      pattern: "goto.*blog"
    - from: "tests/e2e/case-studies.spec.ts"
      to: "/case-studies"
      via: "page.goto('/case-studies')"
      pattern: "goto.*case-studies"
---

<objective>
Create the Wave 0 test scaffolding for all Phase 2 routes and requirements. This plan exists because the Nyquist rule requires automated verify targets to be present before implementation tasks reference them.

Purpose: Establish test contracts that define what "done" looks like for every Phase 2 code task. Tests start failing (routes don't exist yet) — that's correct. Implementation plans make them pass.
Output: Three new e2e spec files + updated homepage.spec.ts with real assertions replacing test.todo() placeholders.
</objective>

<execution_context>
@/Users/NickCavalancia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/NickCavalancia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-and-proof-layer/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create audience, blog, and case-studies e2e spec stubs</name>
  <files>tests/e2e/audience.spec.ts, tests/e2e/blog.spec.ts, tests/e2e/case-studies.spec.ts</files>
  <action>
Create three new Playwright e2e spec files. These tests define the acceptance criteria for routes that do not yet exist — they will fail until the implementation plans run.

**tests/e2e/audience.spec.ts** — covers AUDN-01, AUDN-02, AUDN-03, AUDN-04:
```typescript
import { test, expect } from '@playwright/test'

test.describe('audience page', () => {
  test('page loads at /audience', async ({ page }) => {
    await page.goto('/audience')
    await expect(page).not.toHaveURL(/\/404/)
    await expect(page.locator('main')).toBeVisible()
  })

  test('MSP and MSSP are named as distinct segments (AUDN-01)', async ({ page }) => {
    await page.goto('/audience')
    await expect(page.locator('main')).toContainText(/MSP/i)
    await expect(page.locator('main')).toContainText(/MSSP/i)
  })

  test('persona cards render for all four segments (AUDN-02)', async ({ page }) => {
    await page.goto('/audience')
    const cards = page.locator('[data-testid="persona-card"]')
    await expect(cards).toHaveCount(4)
  })

  test('audience stats section renders with methodology note (AUDN-03)', async ({ page }) => {
    await page.goto('/audience')
    await expect(page.locator('[data-testid="audience-stats"]')).toBeVisible()
    await expect(page.locator('[data-testid="methodology-note"]')).toBeVisible()
  })

  test('industry breakdown and company size distribution render (AUDN-04)', async ({ page }) => {
    await page.goto('/audience')
    await expect(page.locator('[data-testid="industry-breakdown"]')).toBeVisible()
    await expect(page.locator('[data-testid="company-size-distribution"]')).toBeVisible()
  })
})
```

**tests/e2e/blog.spec.ts** — covers BLOG-01, BLOG-02, BLOG-03:
```typescript
import { test, expect } from '@playwright/test'

test.describe('blog listing page', () => {
  test('listing page loads at /blog (BLOG-01)', async ({ page }) => {
    await page.goto('/blog')
    await expect(page).not.toHaveURL(/\/404/)
    await expect(page.locator('main')).toBeVisible()
  })

  test('blog cards have correct data-testid (BLOG-02)', async ({ page }) => {
    await page.goto('/blog')
    // When posts exist, they render as blog-card elements
    const cards = page.locator('[data-testid="blog-card"]')
    // Count check is only enforced after content is published (BLOG-03 is a content gate)
    await expect(cards.first()).toBeVisible()
  })

  test('at least 6 posts appear on listing page (BLOG-03)', async ({ page }) => {
    await page.goto('/blog')
    const cards = page.locator('[data-testid="blog-card"]')
    await expect(cards).toHaveCount(6, { timeout: 10000 })
  })

  test('individual post page renders (BLOG-01)', async ({ page }) => {
    await page.goto('/blog')
    // Navigate to first post
    const firstLink = page.locator('[data-testid="blog-card"] a').first()
    const href = await firstLink.getAttribute('href')
    if (href) {
      await page.goto(href)
      await expect(page.locator('article')).toBeVisible()
    }
  })
})
```

**tests/e2e/case-studies.spec.ts** — covers CASE-01, CASE-02, CASE-03:
```typescript
import { test, expect } from '@playwright/test'

test.describe('case studies', () => {
  test('listing page loads at /case-studies (CASE-01)', async ({ page }) => {
    await page.goto('/case-studies')
    await expect(page).not.toHaveURL(/\/404/)
    await expect(page.locator('main')).toBeVisible()
  })

  test('at least 3 case study cards render (CASE-01)', async ({ page }) => {
    await page.goto('/case-studies')
    const cards = page.locator('[data-testid="case-study-card"]')
    await expect(cards).toHaveCount(3, { timeout: 10000 })
  })

  test('individual case study page renders with metrics (CASE-02)', async ({ page }) => {
    await page.goto('/case-studies')
    const firstLink = page.locator('[data-testid="case-study-card"] a').first()
    const href = await firstLink.getAttribute('href')
    if (href) {
      await page.goto(href)
      await expect(page.locator('[data-testid="case-study-metrics"]')).toBeVisible()
    }
  })

  test('homepage has a case study highlight section (CASE-03)', async ({ page }) => {
    await page.goto('/')
    await expect(page.locator('[data-testid="case-study-highlight"]')).toBeVisible()
  })
})
```

Note: BLOG-03 and CASE-01 count assertions require real Sanity content published to the Vercel preview URL. These will pass only after content is published in Wave 4.
  </action>
  <verify>
    <automated>cd /Users/NickCavalancia/Documents/GitHub/ab2bm-website && npx playwright test tests/e2e/audience.spec.ts tests/e2e/blog.spec.ts tests/e2e/case-studies.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>Three spec files exist and are syntactically valid Playwright specs. Tests run (and fail because routes don't exist yet — that is the correct state for Wave 0 stubs).</done>
</task>

<task type="auto">
  <name>Task 2: Implement homepage.spec.ts test.todo() placeholders</name>
  <files>tests/e2e/homepage.spec.ts</files>
  <action>
The existing `tests/e2e/homepage.spec.ts` has `test.todo()` placeholders for logo strip, programs overview, audience stats, and social proof sections. Replace those with real assertions.

Replace the entire file content. Keep existing passing tests intact. Implement the four todo groups:

**logo strip** (TRST-01): Test that the logo strip section is visible on the homepage. Use `[data-testid="logo-strip"]` locator.

**programs overview**: Test that the programs overview section renders and contains links to `/programs`.

**audience stats**: Test that `[data-testid="audience-stats"]` is visible on the homepage.

**social proof** (TRST-03): Test that `[data-testid="testimonial-highlight"]` renders with attribution text visible.

Also add a test for the case study highlight (CASE-03) in the social proof group or a new group: `[data-testid="case-study-highlight"]` should be visible after case studies are published.

Keep the existing passing tests:
- hero section test (renders with IT decision-makers and MSPs/MSSPs named)
- primary CTA test
- blog preview hidden when no posts test
- footer CTA test

The `blog preview section is hidden when no posts exist` test should stay as-is — it verifies Phase 1 behavior (BlogPreview returns null for empty array). After blog is wired in Phase 2, update this test only if blog preview behavior changes.
  </action>
  <verify>
    <automated>cd /Users/NickCavalancia/Documents/GitHub/ab2bm-website && npm run build 2>&1 | tail -10 && npx playwright test tests/e2e/homepage.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>homepage.spec.ts has no test.todo() calls remaining. All four previously-stubbed test groups have real assertions. Existing passing tests still pass.</done>
</task>

</tasks>

<verification>
Run `npm run build` to confirm no TypeScript errors introduced. Run `npx playwright test tests/e2e/ --reporter=list` to confirm all spec files are syntactically valid and discoverable by the test runner. It is expected that most Phase 2 tests fail at this point — routes and data don't exist yet.
</verification>

<success_criteria>
- tests/e2e/audience.spec.ts, blog.spec.ts, case-studies.spec.ts all exist with valid Playwright syntax
- tests/e2e/homepage.spec.ts has zero test.todo() calls
- `npm run build` passes
- All spec files are discovered by `npx playwright test --list`
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-and-proof-layer/02-01-SUMMARY.md`
</output>
