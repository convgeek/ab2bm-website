---
phase: 02-content-and-proof-layer
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/app/(site)/blog/page.tsx
  - src/app/(site)/blog/[slug]/page.tsx
autonomous: true
requirements:
  - BLOG-01
  - BLOG-02
  - BLOG-04

must_haves:
  truths:
    - "A visitor can navigate to /blog and see a listing of published blog posts"
    - "A visitor can click a post and read the full article at /blog/[slug]"
    - "Visiting /blog/nonexistent-slug returns a 404 page, not a blank page"
    - "Blog routes build statically and handle no-posts state gracefully"
  artifacts:
    - path: "src/app/(site)/blog/page.tsx"
      provides: "Blog listing page — fetches BLOG_LISTING_QUERY, renders blog-card grid"
      exports: []
    - path: "src/app/(site)/blog/[slug]/page.tsx"
      provides: "Individual blog post page — fetches BLOG_POST_QUERY by slug, renders PortableText body"
      exports: ["generateStaticParams", "default"]
  key_links:
    - from: "src/app/(site)/blog/[slug]/page.tsx"
      to: "src/sanity/lib/queries.ts"
      via: "import { BLOG_POST_QUERY } from '@/sanity/lib/queries'"
      pattern: "BLOG_POST_QUERY"
    - from: "src/app/(site)/blog/[slug]/page.tsx"
      to: "notFound()"
      via: "if (!post) notFound()"
      pattern: "notFound"
---

<objective>
Build the blog listing page and individual post pages at /blog and /blog/[slug]. These routes complete BLOG-01 and BLOG-02 — the structural requirements for the blog section. BLOG-03 (6+ posts published) and BLOG-04 (content targeting IT pros) are content requirements fulfilled when CG publishes content in Sanity Studio.

Purpose: Give the blog routes a working implementation that fetches from Sanity, renders gracefully when empty, and handles all edge cases (no posts, missing slug, invalid slug → 404).
Output: Two Next.js App Router page files under src/app/(site)/blog/.
</objective>

<execution_context>
@/Users/NickCavalancia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/NickCavalancia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-content-and-proof-layer/02-RESEARCH.md
@src/sanity/lib/queries.ts
@src/sanity/lib/live.ts
@src/app/(site)/page.tsx

<interfaces>
From src/sanity/lib/queries.ts (added in 02-02):
```typescript
export const BLOG_LISTING_QUERY  // returns { _id, title, slug, excerpt, publishedAt, author }[]
export const BLOG_POST_QUERY     // params: { slug: string } — returns single post with body
export const BLOG_PREVIEW_QUERY  // top 3 posts for homepage
```

From src/sanity/lib/live.ts:
```typescript
export const { sanityFetch, SanityLive } = defineLive({ ... })
// Usage: const { data } = await sanityFetch({ query: QUERY, params: { slug } })
```

Next.js 15 CRITICAL: params is a Promise — must be awaited:
```typescript
export default async function Page({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build blog listing page at /blog</name>
  <files>src/app/(site)/blog/page.tsx</files>
  <action>
Create `src/app/(site)/blog/page.tsx`. This is a Server Component that fetches all published posts via `BLOG_LISTING_QUERY` and renders them as a grid of cards.

Implementation requirements:
- Wrap `sanityFetch` in try/catch with empty array fallback (same pattern as homepage.tsx — handles Sanity not provisioned)
- Render a `<main>` with a page heading "Resources" (or "Blog") and a responsive grid of post cards
- Each post card: `data-testid="blog-card"` (required by audience.spec.ts + blog.spec.ts tests), shows title, excerpt, formatted publishedAt date, and a `<Link href={/blog/${post.slug}}>` wrapping the title
- Empty state: when posts array is empty, show a meaningful empty state message ("Content coming soon") — do not crash or show a blank grid
- Date formatting: `new Date(post.publishedAt).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })`
- Card layout: `rounded-xl border border-border bg-card p-6` (consistent with Phase 1 card styling)
- Grid: `grid grid-cols-1 gap-8 md:grid-cols-2 lg:grid-cols-3`
- Page heading: `text-4xl font-bold tracking-tight`
- Author attribution visible on card when present

Do NOT use `export const dynamic = 'force-dynamic'` — prefer static generation with `defineLive()` ISR (per RESEARCH.md anti-pattern note).
  </action>
  <verify>
    <automated>cd /Users/NickCavalancia/Documents/GitHub/ab2bm-website && npm run build 2>&1 | tail -15</automated>
  </verify>
  <done>/blog route builds without TypeScript errors. Empty-state renders correctly when Sanity has no posts. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Build individual blog post page at /blog/[slug]</name>
  <files>src/app/(site)/blog/[slug]/page.tsx</files>
  <action>
Create `src/app/(site)/blog/[slug]/page.tsx`. This is a dynamic route Server Component.

Implementation requirements:
- `generateStaticParams`: calls `sanityFetch` with an inline `defineQuery` for all post slugs (`*[_type == "post"]{ "slug": slug.current }`), applies `?? []` before mapping (prevents build crash when no posts exist per Pitfall 2)
- Page component: params typed as `Promise<{ slug: string }>`, destructure after `await params` (Next.js 15 Pitfall 1)
- Fetch `BLOG_POST_QUERY` with `params: { slug }` — wrap in try/catch with null fallback
- Call `notFound()` immediately when post is null (Pitfall 3)
- Render: `<article>` element with post title as `<h1>`, formatted publish date, author name, and `<PortableText value={post.body} />` for body
- PortableText components: provide custom `h2` and `blockquote` renderers matching site typography (h2: `text-2xl font-bold mt-8 mb-4`, blockquote: `border-l-4 border-primary pl-4 italic my-4`)
- Import `PortableText` from `@portabletext/react`
- Page wrapper: `<article className="mx-auto max-w-3xl px-4 sm:px-6 py-16">`
- No `export const dynamic = 'force-dynamic'`

Also add a back link: `<Link href="/blog" className="...">← Back to Resources</Link>` above the article title.
  </action>
  <verify>
    <automated>cd /Users/NickCavalancia/Documents/GitHub/ab2bm-website && npm run build 2>&1 | tail -15</automated>
  </verify>
  <done>/blog/[slug] route builds. generateStaticParams handles empty Sanity dataset. notFound() is called for missing slugs. PortableText renders body. Build passes.</done>
</task>

</tasks>

<verification>
`npm run build` passes. `/blog` and `/blog/[slug]` routes appear in the build output. Run `npx playwright test tests/e2e/blog.spec.ts --reporter=list` — the "listing page loads at /blog" test should pass; count-based tests will fail until content is published (expected).
</verification>

<success_criteria>
- src/app/(site)/blog/page.tsx exists with data-testid="blog-card" on post cards
- src/app/(site)/blog/[slug]/page.tsx exists with generateStaticParams and await params
- `npm run build` passes
- Empty state renders without errors when Sanity has no posts
- /blog/nonexistent-slug returns 404 (notFound() called)
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-and-proof-layer/02-03-SUMMARY.md`
</output>
