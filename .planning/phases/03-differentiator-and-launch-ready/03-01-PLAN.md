---
phase: 03-differentiator-and-launch-ready
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/methodology.spec.ts
  - tests/e2e/programs.spec.ts
autonomous: true
requirements:
  - METH-01
  - METH-02
  - METH-03
  - METH-04

must_haves:
  truths:
    - "methodology.spec.ts exists with three failing tests covering each methodology section"
    - "programs.spec.ts covers 6 branded program pages, nav dropdown, and individual routes"
  artifacts:
    - path: "tests/e2e/methodology.spec.ts"
      provides: "Failing test stubs for METH-01, METH-02, METH-03"
      exports: []
    - path: "tests/e2e/programs.spec.ts"
      provides: "Tests for METH-04 — branded names, 6 programs, nav dropdown"
      exports: []
  key_links:
    - from: "tests/e2e/methodology.spec.ts"
      to: "/methodology"
      via: "page.goto('/methodology')"
      pattern: "goto.*methodology"
    - from: "tests/e2e/programs.spec.ts"
      to: "/programs/advance-engage"
      via: "page.goto('/programs/advance-engage')"
      pattern: "goto.*programs/advance"
---

<objective>
Wave 0 test scaffolding — create the Playwright test files that will gate all Phase 3 implementation tasks.

Purpose: Per the Nyquist Rule, every implementation task needs an automated verify command. These test files must exist (and fail) before implementation begins — they define what "done" means for METH-01 through METH-04.
Output: Two test files with data-testid-anchored assertions ready to run against the implementation plans that follow.
</objective>

<execution_context>
@/Users/NickCavalancia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/NickCavalancia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-differentiator-and-launch-ready/03-CONTEXT.md
@.planning/phases/03-differentiator-and-launch-ready/03-VALIDATION.md

<interfaces>
<!-- Established data-testid conventions from Phase 2 (STATE.md decisions) -->
<!-- Methodology sections (to be created in Plan 04): -->
<!--   data-testid="methodology-audience-building" -->
<!--   data-testid="methodology-content-syndication" -->
<!--   data-testid="methodology-webinar" -->
<!-- Programs index (to be created in Plan 05): -->
<!--   data-testid="programs-grid" -->
<!-- Individual program pages (to be created in Plan 03): -->
<!--   data-testid="program-hero" -->
<!--   data-testid="program-detail" -->
<!-- Nav dropdown (to be created in Plan 05): -->
<!--   data-testid="programs-dropdown" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create methodology.spec.ts with section visibility stubs</name>
  <files>tests/e2e/methodology.spec.ts</files>
  <action>
Create `tests/e2e/methodology.spec.ts` covering METH-01, METH-02, METH-03. Each test navigates to `/methodology` and checks that the relevant section container is visible and non-empty.

Use `data-testid` locators (canonical Phase 2+ strategy per STATE.md decisions):
- `data-testid="methodology-audience-building"` — METH-01
- `data-testid="methodology-content-syndication"` — METH-02
- `data-testid="methodology-webinar"` — METH-03

Test structure:

```typescript
import { test, expect } from '@playwright/test'

test.describe('methodology page', () => {
  test('renders methodology page', async ({ page }) => {
    await page.goto('/methodology')
    await expect(page).toHaveURL('/methodology')
    await expect(page.locator('h1')).toBeVisible()
  })
})

test.describe('audience building section (METH-01)', () => {
  test('audience building section is visible and non-empty', async ({ page }) => {
    await page.goto('/methodology')
    const section = page.locator('[data-testid="methodology-audience-building"]')
    await expect(section).toBeVisible()
    await expect(section).not.toBeEmpty()
  })
})

test.describe('content syndication process section (METH-02)', () => {
  test('content syndication section is visible and non-empty', async ({ page }) => {
    await page.goto('/methodology')
    const section = page.locator('[data-testid="methodology-content-syndication"]')
    await expect(section).toBeVisible()
    await expect(section).not.toBeEmpty()
  })
})

test.describe('webinar program process section (METH-03)', () => {
  test('webinar program process section is visible and non-empty', async ({ page }) => {
    await page.goto('/methodology')
    const section = page.locator('[data-testid="methodology-webinar"]')
    await expect(section).toBeVisible()
    await expect(section).not.toBeEmpty()
  })
})
```

These tests MUST FAIL when run before Plan 04 creates the methodology route — that is the correct Wave 0 state.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/methodology.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>File exists at tests/e2e/methodology.spec.ts; tests fail with "ERR_CONNECTION_REFUSED" or 404 (not a syntax/compile error). Running `npx tsc --noEmit` passes with no type errors in the spec file.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite programs.spec.ts to cover METH-04 branded names and 6 programs</name>
  <files>tests/e2e/programs.spec.ts</files>
  <action>
Replace the existing `tests/e2e/programs.spec.ts` entirely. The old tests target `#content-syndication` and `#webinar` anchor IDs which will not exist after the programs index is refactored. The new tests cover METH-04: 6 branded program pages, nav dropdown, individual page routes, and CONV-05 CTA routing.

New test structure:

```typescript
import { test, expect } from '@playwright/test'

const PROGRAM_SLUGS = [
  'advance-engage',
  'advance-abm',
  'advance-install',
  'advance-bant',
  'advance-expand',
  'webinar',
]

const BRANDED_NAMES = [
  'ADVANCE ENGAGE',
  'ADVANCE ABM',
  'ADVANCE INSTALL',
  'ADVANCE BANT',
  'ADVANCE EXPAND',
  'Webinar',
]

test.describe('programs index page (METH-04)', () => {
  test('programs index renders overview grid', async ({ page }) => {
    await page.goto('/programs')
    await expect(page).toHaveURL('/programs')
    const grid = page.locator('[data-testid="programs-grid"]')
    await expect(grid).toBeVisible()
  })

  test('programs index shows all 6 branded program names', async ({ page }) => {
    await page.goto('/programs')
    for (const name of BRANDED_NAMES) {
      await expect(page.getByText(name, { exact: false })).toBeVisible()
    }
  })
})

test.describe('individual program pages (METH-04)', () => {
  for (const slug of PROGRAM_SLUGS) {
    test(`/programs/${slug} renders program hero`, async ({ page }) => {
      await page.goto(`/programs/${slug}`)
      await expect(page).toHaveURL(`/programs/${slug}`)
      const hero = page.locator('[data-testid="program-hero"]')
      await expect(hero).toBeVisible()
    })
  }
})

test.describe('program CTA routing (CONV-05)', () => {
  for (const slug of PROGRAM_SLUGS) {
    test(`/programs/${slug} CTA links to contact with program param`, async ({ page }) => {
      await page.goto(`/programs/${slug}`)
      const main = page.locator('main')
      const ctaLinks = main.getByRole('link', { name: /conversation|inquire|contact|get started/i })
      const count = await ctaLinks.count()
      expect(count).toBeGreaterThanOrEqual(1)
      const firstHref = await ctaLinks.first().getAttribute('href')
      expect(firstHref).toContain('program=')
    })
  }
})

test.describe('nav dropdown (METH-04)', () => {
  test('programs nav dropdown is present in desktop nav', async ({ page }) => {
    await page.goto('/')
    const dropdown = page.locator('[data-testid="programs-dropdown"]')
    await expect(dropdown).toBeVisible()
  })

  test('programs dropdown lists all 6 program links', async ({ page }) => {
    await page.goto('/')
    const dropdown = page.locator('[data-testid="programs-dropdown"]')
    await dropdown.hover()
    for (const slug of PROGRAM_SLUGS) {
      const link = page.locator(`a[href="/programs/${slug}"]`).first()
      await expect(link).toBeVisible()
    }
  })
})
```

These tests MUST FAIL before Plans 03 and 05 are executed — that is correct Wave 0 behavior.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/programs.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>File exists at tests/e2e/programs.spec.ts; tests compile cleanly (`npx tsc --noEmit` passes); tests fail against the current site (which has no /programs/advance-engage or dropdown) — not due to syntax errors.</done>
</task>

</tasks>

<verification>
After both tasks:
- `npx tsc --noEmit` — no TypeScript errors in spec files
- Both spec files exist and contain tests
- Tests fail against the current site (expected — implementation not yet built)
</verification>

<success_criteria>
Two Wave 0 test files exist and define the acceptance criteria for Plans 03, 04, and 05. Tests are syntactically valid but fail because the routes/components they target do not yet exist.
</success_criteria>

<output>
After completion, create `.planning/phases/03-differentiator-and-launch-ready/03-01-SUMMARY.md`
</output>
