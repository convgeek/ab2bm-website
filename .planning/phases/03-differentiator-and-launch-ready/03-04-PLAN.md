---
phase: 03-differentiator-and-launch-ready
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/app/(site)/methodology/page.tsx
  - src/components/sections/MethodologyPage.tsx
autonomous: true
requirements:
  - METH-01
  - METH-02
  - METH-03
  - METH-04

must_haves:
  truths:
    - "Visiting /methodology renders the page with all three section containers visible"
    - "The Audience Building section is non-empty (fallback content renders before Sanity content)"
    - "The Content Syndication Process section references 'ADVANCE ENGAGE' by name"
    - "The Webinar Program Process section is present and non-empty"
    - "All three sections have data-testid attributes so Playwright tests pass"
  artifacts:
    - path: "src/app/(site)/methodology/page.tsx"
      provides: "Singleton methodology page using METHODOLOGY_PAGE_QUERY + defineLive ISR"
      exports: ["default"]
    - path: "src/components/sections/MethodologyPage.tsx"
      provides: "Three-section methodology content component"
      exports: ["MethodologyPage"]
  key_links:
    - from: "src/app/(site)/methodology/page.tsx"
      to: "src/sanity/lib/queries.ts"
      via: "METHODOLOGY_PAGE_QUERY"
      pattern: "METHODOLOGY_PAGE_QUERY"
    - from: "src/components/sections/MethodologyPage.tsx"
      to: "data-testid attributes"
      via: "methodology-audience-building, methodology-content-syndication, methodology-webinar"
      pattern: "data-testid"
---

<objective>
Methodology page — build the /methodology route and its content component covering how Ab2bm builds its audience and how each program type delivers leads (METH-01, METH-02, METH-03).

Purpose: This page answers the question "how do you source and validate leads?" — the primary credibility question from prospective clients. It distinguishes Ab2bm's opt-in audience from scraped databases and explains mechanics in plain English for all three program types.
Output: /methodology route (Sanity singleton, ISR) + MethodologyPage section component with three labeled sections and fallback content.
</objective>

<execution_context>
@/Users/NickCavalancia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/NickCavalancia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-differentiator-and-launch-ready/03-CONTEXT.md
@.planning/phases/03-differentiator-and-launch-ready/03-RESEARCH.md
@docs/AB2B_Services_Portfolio.md

<interfaces>
<!-- From Plan 02 (03-02-SUMMARY.md): -->
<!-- METHODOLOGY_PAGE_QUERY shape: { pageHeadline, pageSubheadline, audienceBuilding, contentSyndicationProcess, webinarProcess } -->
<!-- All three process fields are PortableText (array of block) — render with <PortableText> from @portabletext/react -->

<!-- methodologyPage Sanity schema (singleton): -->
<!-- __experimental_actions: ['update', 'publish'] — no Create New in Studio -->

<!-- Established patterns (STATE.md): -->
<!-- sanityFetch wrapped in try/catch — build passes without credentials -->
<!-- data-testid locators canonical -->
<!-- Section components are pure presentational (props-only) -->
<!-- defineLive ISR — no export const dynamic -->

<!-- Methodology page sections (CONTEXT.md locked decisions): -->
<!-- Section 1: "Audience Building" -->
<!-- Section 2: "ADVANCE ENGAGE / Content Syndication Process" -->
<!-- Section 3: "Webinar Program Process" -->
<!-- Tone: mechanics-first, plain English — step-by-step, no marketing fluff -->
<!-- Source: portfolio doc "Audience & Infrastructure" section (6-activity model) -->
<!--         portfolio doc ADVANCE ENGAGE "How the Program Works" -->
<!--         Webinar — use Phase 1 fallback webinar copy structured to match ADVANCE format -->

<!-- RESEARCH.md Pitfall 7: methodology sections MUST have data-testid always rendered -->
<!-- Even with empty Sanity state, the three containers must be present so Playwright locators work -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MethodologyPage section component</name>
  <files>src/components/sections/MethodologyPage.tsx</files>
  <action>
Create a pure presentational component. Named export `MethodologyPage`.

Props interface:
```typescript
interface MethodologyPageProps {
  pageHeadline: string
  pageSubheadline: string | null
  audienceBuilding: PortableTextBlock[] | null
  contentSyndicationProcess: PortableTextBlock[] | null
  webinarProcess: PortableTextBlock[] | null
}
```

Import `PortableText` from `@portabletext/react` and `PortableTextBlock` from `@portabletext/react` (already installed).

**JSX structure:**

```tsx
<div>
  {/* Page header */}
  <div className="bg-background py-16 md:py-24 border-b border-border">
    <div className="mx-auto max-w-4xl px-4 sm:px-6 lg:px-8">
      <h1 className="text-4xl font-bold tracking-tight text-foreground sm:text-5xl">
        {pageHeadline}
      </h1>
      {pageSubheadline && (
        <p className="mt-6 text-xl text-muted-foreground leading-relaxed max-w-2xl">
          {pageSubheadline}
        </p>
      )}
    </div>
  </div>

  {/* Section 1: Audience Building */}
  <section
    data-testid="methodology-audience-building"
    className="py-16 border-b border-border"
  >
    <div className="mx-auto max-w-4xl px-4 sm:px-6 lg:px-8">
      <h2 className="text-2xl font-bold text-foreground mb-6">Audience Building</h2>
      {audienceBuilding ? (
        <div className="prose prose-neutral max-w-none">
          <PortableText value={audienceBuilding} />
        </div>
      ) : (
        // Fallback content — always renders so data-testid locator works in tests
        <div className="space-y-4 text-muted-foreground">
          <p>Ab2bm's audience is built — not bought. The foundation is a decade of opt-in relationship building with B2B professionals across the IT ecosystem.</p>
          <p>The audience-building methodology centers on six interconnected activities:</p>
          <ul className="list-disc list-inside space-y-2 ml-4">
            <li><strong>Account Coverage</strong> — ensuring the right companies are represented across target markets</li>
            <li><strong>Key Contacts, Roles &amp; Titles</strong> — mapping decision-makers and buying committee members</li>
            <li><strong>Opt-In Outreach</strong> — putting the B2B professional in control of the engagement</li>
            <li><strong>Info Validation &amp; Verification</strong> — confirming professional information is current and accurate</li>
            <li><strong>Data/Record Enhancement</strong> — adding data and intelligence as professionals engage</li>
            <li><strong>Audience Segmentation</strong> — enabling precise targeting for every program</li>
          </ul>
          <p>Every audience member has explicitly opted in and granted permission for marketing outreach. Opt-in records are captured and retained for verification and audit purposes.</p>
        </div>
      )}
    </div>
  </section>

  {/* Section 2: Content Syndication Process (ADVANCE ENGAGE) */}
  <section
    data-testid="methodology-content-syndication"
    className="py-16 border-b border-border bg-muted/30"
  >
    <div className="mx-auto max-w-4xl px-4 sm:px-6 lg:px-8">
      <h2 className="text-2xl font-bold text-foreground mb-6">ADVANCE ENGAGE / Content Syndication Process</h2>
      {contentSyndicationProcess ? (
        <div className="prose prose-neutral max-w-none">
          <PortableText value={contentSyndicationProcess} />
        </div>
      ) : (
        // Fallback content
        <div className="space-y-4 text-muted-foreground">
          <p>ADVANCE ENGAGE works by putting high-value, educational content to work engaging B2B professionals who meet your target profile. Here is how a campaign runs from start to lead delivery:</p>
          <ol className="list-decimal list-inside space-y-2 ml-4">
            <li><strong>Campaign Brief</strong> — The Ab2bm team works with you to define the target buyer profile, ICP criteria, and content assets to promote.</li>
            <li><strong>Audience Targeting</strong> — Target accounts and buyer roles are matched against the opt-in audience using your criteria: geography, job function, company size, industry, and installed tech.</li>
            <li><strong>Multi-Channel Outreach</strong> — Ab2bm uses multiple communications channels to engage buyers on their terms — delivering your content to the right professionals at the right time.</li>
            <li><strong>Lead Capture &amp; Qualification</strong> — Professionals who engage with the content opt in, confirm their business information, and answer any required profiling questions.</li>
            <li><strong>Quality Review</strong> — Every lead is reviewed by the Ab2bm quality team before delivery. Leads that do not meet criteria are replaced.</li>
            <li><strong>Lead Delivery</strong> — Verified leads are delivered to your system of choice with full contact data, engagement details, and any profiling answers collected.</li>
          </ol>
        </div>
      )}
    </div>
  </section>

  {/* Section 3: Webinar Program Process */}
  <section
    data-testid="methodology-webinar"
    className="py-16"
  >
    <div className="mx-auto max-w-4xl px-4 sm:px-6 lg:px-8">
      <h2 className="text-2xl font-bold text-foreground mb-6">Webinar Program Process</h2>
      {webinarProcess ? (
        <div className="prose prose-neutral max-w-none">
          <PortableText value={webinarProcess} />
        </div>
      ) : (
        // Fallback content
        <div className="space-y-4 text-muted-foreground">
          <p>Ab2bm's webinar program connects your subject matter experts directly with senior IT buyers via co-branded virtual events. Here is how a webinar program runs:</p>
          <ol className="list-decimal list-inside space-y-2 ml-4">
            <li><strong>Program Setup</strong> — Ab2bm works with you to define the webinar topic, format, speaker lineup, and target audience.</li>
            <li><strong>Co-Branded Promotion</strong> — Ab2bm promotes the event to its opt-in audience using email, multi-channel outreach, and targeted engagement — driving qualified registrations.</li>
            <li><strong>Registration &amp; Confirmation</strong> — Registrants confirm their attendance, validate their contact information, and are reminded in the lead-up to the event.</li>
            <li><strong>Event Delivery</strong> — The webinar runs on a platform selected in the program brief. Ab2bm supports with promotion, audience management, and registration logistics.</li>
            <li><strong>Lead Delivery</strong> — Post-event, Ab2bm delivers the full registrant and attendee list with contact data, registration details, and engagement indicators (attended, did not attend).</li>
          </ol>
        </div>
      )}
    </div>
  </section>
</div>
```

Named export only — no default export from this component file.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Component file exists, compiles cleanly, exports named MethodologyPage function, and all three data-testid attributes are present on section elements.</done>
</task>

<task type="auto">
  <name>Task 2: Create /methodology page route</name>
  <files>src/app/(site)/methodology/page.tsx</files>
  <action>
Create `src/app/(site)/methodology/page.tsx` as a Server Component following the audiencePage singleton pattern.

**Metadata:**
```typescript
export const metadata: Metadata = {
  title: 'Methodology | Advance B2B Media',
  description: 'How Ab2bm builds, engages, and qualifies its opt-in audience — and how content syndication and webinar programs deliver leads.',
}
```

**Fallback data (render when Sanity not configured):**
```typescript
const FALLBACK_METHODOLOGY = {
  pageHeadline: 'How We Deliver Results',
  pageSubheadline: 'Ab2bm\'s demand programs are built on a first-party, opt-in audience built over more than a decade. Here is exactly how the audience is built, how programs run, and how leads are qualified and delivered.',
  audienceBuilding: null,
  contentSyndicationProcess: null,
  webinarProcess: null,
}
```

**Fetch logic:**
```typescript
let methodologyData = FALLBACK_METHODOLOGY

try {
  const result = await sanityFetch({ query: METHODOLOGY_PAGE_QUERY })
  if (result.data) {
    methodologyData = {
      pageHeadline: result.data.pageHeadline ?? FALLBACK_METHODOLOGY.pageHeadline,
      pageSubheadline: result.data.pageSubheadline ?? FALLBACK_METHODOLOGY.pageSubheadline,
      audienceBuilding: (result.data.audienceBuilding as PortableTextBlock[] | null) ?? null,
      contentSyndicationProcess: (result.data.contentSyndicationProcess as PortableTextBlock[] | null) ?? null,
      webinarProcess: (result.data.webinarProcess as PortableTextBlock[] | null) ?? null,
    }
  }
} catch {
  // Sanity not configured — fallback renders placeholder content
}
```

**JSX:**
```tsx
return (
  <main>
    <MethodologyPage
      pageHeadline={methodologyData.pageHeadline}
      pageSubheadline={methodologyData.pageSubheadline}
      audienceBuilding={methodologyData.audienceBuilding}
      contentSyndicationProcess={methodologyData.contentSyndicationProcess}
      webinarProcess={methodologyData.webinarProcess}
    />
  </main>
)
```

Imports needed: `Metadata` from `next`, `sanityFetch` from `@/sanity/lib/live`, `METHODOLOGY_PAGE_QUERY` from `@/sanity/lib/queries`, `PortableTextBlock` from `@portabletext/react`, `MethodologyPage` from `@/components/sections/MethodologyPage`.

Do NOT use `export const dynamic`. No searchParams. Static generation via defineLive ISR.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/methodology.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `/methodology` returns 200 in dev server
    - `npm run build` includes `/methodology` in the static routes
    - All 4 methodology.spec.ts tests pass (page renders, 3 section containers visible and non-empty with fallback content)
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — zero errors
- `npm run build` — succeeds; /methodology appears in output
- `npx playwright test tests/e2e/methodology.spec.ts --reporter=list` — all 4 tests green
</verification>

<success_criteria>
/methodology renders all three labeled sections (Audience Building, ADVANCE ENGAGE / Content Syndication Process, Webinar Program Process) with fallback content that satisfies METH-01, METH-02, METH-03. The content explicitly references ADVANCE ENGAGE by name in the content syndication section (METH-04). Ab2bm can update all content in Sanity Studio without touching code.
</success_criteria>

<output>
After completion, create `.planning/phases/03-differentiator-and-launch-ready/03-04-SUMMARY.md`
</output>
