---
phase: 03-differentiator-and-launch-ready
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/app/(site)/programs/[slug]/page.tsx
  - src/components/sections/ProgramHero.tsx
  - src/components/sections/ProgramDetail.tsx
  - src/components/sections/OptionalIngredients.tsx
  - src/components/sections/ComplianceBlock.tsx
  - src/components/sections/ProgramManagement.tsx
autonomous: true
requirements:
  - METH-04

must_haves:
  truths:
    - "Visiting /programs/advance-engage renders a page with a program hero, solution overview, how it works, and CTA"
    - "All 6 program slugs return pages (not 404) even when Sanity is unconfigured"
    - "Each program CTA links to /contact?program={programType} (CONV-05 pattern)"
    - "Optional Ingredients, Compliance, and Program Management blocks appear on each program page"
  artifacts:
    - path: "src/app/(site)/programs/[slug]/page.tsx"
      provides: "Dynamic program page route serving all 6 programs"
      exports: ["default", "generateStaticParams"]
    - path: "src/components/sections/ProgramHero.tsx"
      provides: "Program name + tagline hero section"
      exports: ["ProgramHero"]
    - path: "src/components/sections/ProgramDetail.tsx"
      provides: "Solution overview, How it Works, What You Get, Best For sections"
      exports: ["ProgramDetail"]
    - path: "src/components/sections/OptionalIngredients.tsx"
      provides: "Shared optional ingredients module (reused across all 6 program pages)"
      exports: ["OptionalIngredients"]
    - path: "src/components/sections/ComplianceBlock.tsx"
      provides: "GDPR/CASL/CCPA compliance section (shared)"
      exports: ["ComplianceBlock"]
    - path: "src/components/sections/ProgramManagement.tsx"
      provides: "24-48hr launch guarantee block (shared)"
      exports: ["ProgramManagement"]
  key_links:
    - from: "src/app/(site)/programs/[slug]/page.tsx"
      to: "src/sanity/lib/queries.ts"
      via: "PROGRAM_BY_SLUG_QUERY, ALL_PROGRAM_SLUGS_QUERY"
      pattern: "PROGRAM_BY_SLUG_QUERY"
    - from: "src/app/(site)/programs/[slug]/page.tsx"
      to: "src/components/sections/ProgramInquiryCta.tsx"
      via: "programType prop → /contact?program={programType}"
      pattern: "ProgramInquiryCta"
---

<objective>
Individual program landing pages — build the dynamic /programs/[slug] route serving all 6 program pages (ADVANCE ENGAGE, ADVANCE ABM, ADVANCE INSTALL, ADVANCE BANT, ADVANCE EXPAND, Webinar) plus the shared section components they use.

Purpose: This is the primary METH-04 deliverable — programs are referred to by branded framework names on dedicated landing pages. Each page follows the portfolio doc structure: Hero → Solution Overview → How it Works → What You Get → Best For → Optional Ingredients → Compliance → Program Management → CTA.
Output: Dynamic route with generateStaticParams, 5 new section components, fallback content for all 6 programs.
</objective>

<execution_context>
@/Users/NickCavalancia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/NickCavalancia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-differentiator-and-launch-ready/03-CONTEXT.md
@.planning/phases/03-differentiator-and-launch-ready/03-RESEARCH.md
@docs/AB2B_Services_Portfolio.md

<interfaces>
<!-- From Plan 02 (03-02-SUMMARY.md for exact exports): -->
<!-- queries.ts new exports: PROGRAM_BY_SLUG_QUERY, ALL_PROGRAM_SLUGS_QUERY, PROGRAMS_INDEX_QUERY, METHODOLOGY_PAGE_QUERY -->
<!-- PROGRAM_BY_SLUG_QUERY shape: { _id, name, slug, tagline, programType, solutionOverview, howItWorks, whatYouGet, bestFor, ctaLabel } -->
<!-- ALL_PROGRAM_SLUGS_QUERY shape: [{ slug: string | null }] -->

<!-- Existing reusable: -->
<!-- ProgramInquiryCta — src/components/sections/ProgramInquiryCta.tsx -->
<!-- Props: { programType: string, ctaLabel: string } — passes ?program={programType} to /contact -->

<!-- Established patterns (STATE.md): -->
<!-- sanityFetch wrapped in try/catch in page.tsx — build passes without Sanity credentials -->
<!-- Explicit any helper functions for GROQ results -->
<!-- data-testid locators are canonical (Phase 2+) -->
<!-- Section components are pure presentational (props-only, no data fetching) -->
<!-- No notFound() on Sanity fetch failure — show fallback; notFound() only for genuinely missing slug -->
<!-- No export const dynamic — static generation via defineLive ISR (RESEARCH.md) -->
<!-- No searchParams on these pages (avoids dynamic rendering opt-out) -->

<!-- Fallback data (from RESEARCH.md Code Examples): -->
<!-- FALLBACK_PROGRAMS: advance-engage, advance-abm, advance-install, advance-bant, advance-expand, webinar -->
<!-- Each has: _id, name, slug, programType, tagline, solutionOverview, howItWorks, whatYouGet, bestFor, ctaLabel -->

<!-- RESEARCH.md Pitfall 4: On Sanity fetch failure, set program = fallback (not notFound()) -->
<!-- RESEARCH.md Pitfall 5: Use next/image for all images (no raw <img> tags) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared program section components</name>
  <files>
    src/components/sections/ProgramHero.tsx,
    src/components/sections/ProgramDetail.tsx,
    src/components/sections/OptionalIngredients.tsx,
    src/components/sections/ComplianceBlock.tsx,
    src/components/sections/ProgramManagement.tsx
  </files>
  <action>
Create 5 pure presentational section components. All props-only — no data fetching (established codebase pattern).

**src/components/sections/ProgramHero.tsx**
Props: `{ name: string; tagline: string; programType: string }`
- Section with `data-testid="program-hero"` on the outer element
- Display program name in h1, tagline as subheadline
- Use a visual accent based on programType (e.g., a subtle colored border or badge) — Claude's discretion on exact style, reuse existing Tailwind classes and color tokens from the project

**src/components/sections/ProgramDetail.tsx**
Props: `{ solutionOverview: string | null; howItWorks: PortableTextBlock[] | null; whatYouGet: PortableTextBlock[] | null; bestFor: string | null }`
- Section with `data-testid="program-detail"`
- Render solutionOverview as a paragraph (fallback: empty string renders nothing)
- Render howItWorks and whatYouGet as labeled subsections using `<PortableText>` from `@portabletext/react` (already installed); if null, render nothing for that subsection
- Render bestFor as a paragraph under a "Best For" heading; if null, render nothing

**src/components/sections/OptionalIngredients.tsx**
No props needed — static content from portfolio doc. List all 8 optional ingredients from `docs/AB2B_Services_Portfolio.md` "Program Optional Ingredients" section:
- LinkedIn Profiles
- Native Language Campaigns
- SIC & NAICS Codes
- Buyer Profiling Questions
- Programmatic Advertising
- Suppression & Exclusion Lists
- Intent Data Signals
- ABM Look-a-Like Modeling

Render as a grid of cards or a styled list. Heading: "Optional Program Ingredients". Use `data-testid="optional-ingredients"`.

**src/components/sections/ComplianceBlock.tsx**
No props — static content from portfolio doc "Data Privacy & Compliance" section:
- Headline: "Compliance-First by Design"
- Tagline: "Lawyers and audiences love our diligence, so will yours."
- Three pillars: Global Compliance (GDPR/CASL/CCPA), Opting In / Permissioning, Opt-In Recording

Use `data-testid="compliance-block"`.

**src/components/sections/ProgramManagement.tsx**
No props — static content from portfolio doc "Program Management & Delivery" section:
- Headline: "Full-Service Program Management"
- Tagline: "Accountable, experienced program managers to deliver your KPIs — working for and with you as coaches and consultants."
- Key stats: 24-48 hours to launch, first lead within 3 days, global time zone coverage, dedicated quality team

Use `data-testid="program-management"`.

All components: Named exports, TypeScript interfaces for props where applicable, Tailwind v4 styling consistent with existing site patterns.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All 5 component files exist, compile cleanly, export named functions, and include data-testid attributes on their root elements.</done>
</task>

<task type="auto">
  <name>Task 2: Create /programs/[slug] dynamic route</name>
  <files>src/app/(site)/programs/[slug]/page.tsx</files>
  <action>
Create `src/app/(site)/programs/[slug]/page.tsx` following the exact same pattern as `src/app/(site)/blog/[slug]/page.tsx`.

**generateStaticParams:**
- Query ALL_PROGRAM_SLUGS_QUERY via sanityFetch
- Wrap in try/catch — return [] on failure (per established pattern)
- Filter null slugs, map to `{ slug: string }`

**params:**
- `params: Promise<{ slug: string }>` — destructure with `await params` (Next.js 15/16 async params)

**Fallback data — define FALLBACK_PROGRAMS array at module level:**
```typescript
const FALLBACK_PROGRAMS = [
  {
    _id: 'fb-engage',
    name: 'ADVANCE ENGAGE',
    slug: 'advance-engage',
    programType: 'advance-engage',
    tagline: 'Top-of-funnel opt-in lead generation built around your content.',
    solutionOverview: 'ADVANCE ENGAGE is built for marketers who need to generate opt-in leads that engage and meet specific business and buyer criteria. It is the go-to program for content syndication and top-of-funnel demand.',
    howItWorks: null,
    whatYouGet: null,
    bestFor: 'Demand generation and content marketing teams that need to fill the top of the funnel with engaged, opt-in prospects.',
    ctaLabel: 'Start a Conversation',
  },
  {
    _id: 'fb-abm',
    name: 'ADVANCE ABM',
    slug: 'advance-abm',
    programType: 'advance-abm',
    tagline: 'Account-based engagement that fuels your ABM strategy.',
    solutionOverview: 'ADVANCE ABM fuels account-based marketing strategies by generating opt-in, engaged buying committee leads within a client\'s target account list.',
    howItWorks: null,
    whatYouGet: null,
    bestFor: 'Marketing teams running account-based strategies who need to identify and engage buying committee members at target accounts.',
    ctaLabel: 'Start a Conversation',
  },
  {
    _id: 'fb-install',
    name: 'ADVANCE INSTALL',
    slug: 'advance-install',
    programType: 'advance-install',
    tagline: 'Leads at organizations with the specific installed technology you need.',
    solutionOverview: 'ADVANCE INSTALL identifies organizations with specific installed technologies and generates engaged, opt-in leads that match the client\'s buyer profile at those organizations.',
    howItWorks: null,
    whatYouGet: null,
    bestFor: 'Brands that market and sell into organizations requiring specific installed technologies.',
    ctaLabel: 'Start a Conversation',
  },
  {
    _id: 'fb-bant',
    name: 'ADVANCE BANT',
    slug: 'advance-bant',
    programType: 'advance-bant',
    tagline: 'Sales-ready leads qualified on Budget, Authority, Need, and Timing.',
    solutionOverview: 'ADVANCE BANT delivers leads that have confirmed Budget, Authority, Need, and Timing — actively looking for solutions in the client\'s market category within the next six months.',
    howItWorks: null,
    whatYouGet: null,
    bestFor: 'Sales-led organizations that need pipeline-ready leads and want to shorten the sales cycle.',
    ctaLabel: 'Start a Conversation',
  },
  {
    _id: 'fb-expand',
    name: 'ADVANCE EXPAND',
    slug: 'advance-expand',
    programType: 'advance-expand',
    tagline: 'Cross-sell, upsell, and expansion inside your current customer base.',
    solutionOverview: 'ADVANCE EXPAND is for marketing teams charged with expanding, cross-selling, and upselling into their current customer bases — reaching new decision-makers within existing accounts.',
    howItWorks: null,
    whatYouGet: null,
    bestFor: 'Customer marketing and expansion-focused teams tasked with growing revenue inside the existing customer base.',
    ctaLabel: 'Start a Conversation',
  },
  {
    _id: 'fb-webinar',
    name: 'Webinar',
    slug: 'webinar',
    programType: 'webinar',
    tagline: 'Connect your experts directly with senior IT buyers via co-branded virtual events.',
    solutionOverview: 'Host co-branded webinars with Advance B2B Media, connecting your subject matter experts directly with senior IT buyers actively evaluating solutions in your market.',
    howItWorks: null,
    whatYouGet: null,
    bestFor: 'Tech vendors who want to engage senior IT buyers in a live, interactive format with direct Q&A and real-time qualification.',
    ctaLabel: 'Start a Conversation',
  },
]
```

**Page component logic:**
1. Fetch program by slug using PROGRAM_BY_SLUG_QUERY with try/catch
2. If Sanity returns null for the slug, find the slug in FALLBACK_PROGRAMS; if not in fallbacks either, call `notFound()`
3. If Sanity throws, use the fallback for that slug

**Page JSX structure:**
```tsx
<main>
  <ProgramHero name={program.name} tagline={program.tagline} programType={program.programType} />
  <ProgramDetail
    solutionOverview={program.solutionOverview}
    howItWorks={program.howItWorks}
    whatYouGet={program.whatYouGet}
    bestFor={program.bestFor}
  />
  <OptionalIngredients />
  <ComplianceBlock />
  <ProgramManagement />
  <section className="py-16 border-t border-border">
    <div className="mx-auto max-w-4xl px-4 sm:px-6 lg:px-8 text-center">
      <ProgramInquiryCta
        programType={program.programType}
        ctaLabel={program.ctaLabel ?? 'Start a Conversation'}
      />
    </div>
  </section>
</main>
```

**Metadata:** Export a `generateMetadata` function that returns `{ title: '{program.name} | Advance B2B Media', description: program.tagline }`.

Do NOT use `export const dynamic`. Do NOT use searchParams. These pages must be statically generated at build time via generateStaticParams.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/programs.spec.ts --reporter=list 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `src/app/(site)/programs/[slug]/page.tsx` exists
    - `npx tsc --noEmit` passes
    - `npm run build` succeeds — 6 program routes pre-rendered
    - Playwright test for program-hero visibility passes for at least `advance-engage` and `webinar`
    - `/programs/advance-engage` and `/programs/webinar` return 200 in dev (`npx next dev` + curl)
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — zero errors
- `npm run build` — succeeds; look for 6 `/programs/[slug]` routes in build output
- `npx playwright test tests/e2e/programs.spec.ts --reporter=list` — program-hero and CTA tests pass; nav dropdown tests still fail (not yet built)
</verification>

<success_criteria>
All 6 branded program landing pages render with fallback content when Sanity is unconfigured. Visiting /programs/advance-engage shows "ADVANCE ENGAGE" in an h1 with tagline and a CTA linking to /contact?program=advance-engage. The optional ingredients, compliance, and program management blocks are present on every program page.
</success_criteria>

<output>
After completion, create `.planning/phases/03-differentiator-and-launch-ready/03-03-SUMMARY.md`
</output>
