---
phase: 03-differentiator-and-launch-ready
plan: 06
type: execute
wave: 5
depends_on:
  - 03-05
files_modified:
  - src/app/layout.tsx
  - package.json
autonomous: false
requirements:
  - INFRA-06

must_haves:
  truths:
    - "Speed Insights is installed and reporting field CWV data from Vercel production"
    - "Site passes Core Web Vitals: LCP < 2.5s, CLS < 0.1, INP < 200ms on all pages"
    - "No raw <img> tags exist on Phase 3 pages (next/image used for all images)"
    - "Fonts use next/font (no layout shift from font loading)"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "@vercel/speed-insights SpeedInsights component added to root layout"
      contains: "SpeedInsights"
    - path: "package.json"
      provides: "@vercel/speed-insights dependency"
      contains: "@vercel/speed-insights"
  key_links:
    - from: "src/app/layout.tsx"
      to: "@vercel/speed-insights/next"
      via: "import SpeedInsights + render in layout"
      pattern: "SpeedInsights"
---

<objective>
Core Web Vitals — install Speed Insights for field CWV data, audit new Phase 3 pages for image/font issues that cause LCP or CLS failures, and perform a human checkpoint to validate CWV on Vercel production.

Purpose: INFRA-06 requires the site to pass LCP < 2.5s, CLS < 0.1, INP < 200ms on Vercel production. This is the final gate before launch. The automated part is the Speed Insights install and audit. The manual part is running Lighthouse on the Vercel preview URL.
Output: Speed Insights installed in root layout; CWV audit complete; human confirms pass on production.
</objective>

<execution_context>
@/Users/NickCavalancia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/NickCavalancia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-differentiator-and-launch-ready/03-CONTEXT.md
@.planning/phases/03-differentiator-and-launch-ready/03-RESEARCH.md

<interfaces>
<!-- RESEARCH.md standard stack: @vercel/speed-insights (latest) -->
<!-- Install: npm install @vercel/speed-insights -->
<!-- Usage: import { SpeedInsights } from '@vercel/speed-insights/next' in layout.tsx -->

<!-- RESEARCH.md "Don't Hand-Roll" table: -->
<!-- Image optimization: next/image — use for any images on new program pages -->
<!-- Font layout shift: next/font — audit root layout; if fonts not using next/font, switch -->

<!-- RESEARCH.md Pitfall 5: CLS from unsized images — use next/image with explicit width/height or fill -->
<!-- RESEARCH.md: "Next.js 16 + Vercel provides most CWV optimization automatically" -->
<!-- Manual audit checklist: img → next/image, fonts → next/font, no layout shift from Phase 3 components -->

<!-- Root layout is at src/app/layout.tsx (top-level App Router layout) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Speed Insights + audit Phase 3 pages for CWV issues</name>
  <files>src/app/layout.tsx, package.json</files>
  <action>
**A. Install @vercel/speed-insights:**
```bash
npm install @vercel/speed-insights
```

**B. Update src/app/layout.tsx:**
Import and render SpeedInsights in the root layout. Add it as the last child inside the `<body>` element (after the main content slot):

```typescript
import { SpeedInsights } from '@vercel/speed-insights/next'
```

In the body JSX:
```tsx
<body>
  {/* ... existing layout content ... */}
  <SpeedInsights />
</body>
```

Read the current layout.tsx first to find the exact insertion point before editing.

**C. Audit Phase 3 pages for CWV issues:**
Scan all Phase 3 files for raw `<img>` tags and non-next/font font patterns:

```bash
grep -rn "<img " src/app/\(site\)/programs/ src/app/\(site\)/methodology/ src/components/sections/ProgramHero.tsx src/components/sections/ProgramDetail.tsx src/components/sections/OptionalIngredients.tsx src/components/sections/ComplianceBlock.tsx src/components/sections/ProgramManagement.tsx src/components/sections/MethodologyPage.tsx 2>/dev/null
```

```bash
grep -n "next/font\|@font-face\|font-family" src/app/layout.tsx
```

For each raw `<img>` found on Phase 3 pages: replace with `next/image` (import Image from 'next/image', add width/height props or use fill with a sized container). Phase 3 program pages and methodology page are text-heavy — if no images exist on these pages, document that in the summary.

If fonts in layout.tsx are NOT using `next/font`, switch them. If they already use next/font (or if no custom fonts are defined), document that the audit passed.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && grep -n "SpeedInsights" src/app/layout.tsx</automated>
  </verify>
  <done>
    - `@vercel/speed-insights` appears in package.json dependencies
    - `src/app/layout.tsx` imports and renders SpeedInsights
    - `npx tsc --noEmit` passes
    - `npm run build` succeeds
    - Audit complete: no raw img tags on Phase 3 pages (or all replaced with next/image), fonts use next/font
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Core Web Vitals checkpoint — verify CWV on Vercel production</name>
  <files>Vercel Dashboard, Chrome DevTools Lighthouse (no code files modified)</files>
  <action>Human verification required: run Lighthouse on Vercel production/preview URL for all key pages and confirm Core Web Vitals thresholds are met. See how-to-verify for full checklist.</action>
  <verify>
    <automated>MANUAL ONLY — INFRA-06 requires production Vercel URL; no automated pre-deploy gate possible (documented in 03-VALIDATION.md)</automated>
  </verify>
  <done>LCP &lt; 2.5s, CLS &lt; 0.1, INP/TBT &lt; 200ms on all key pages confirmed green in Lighthouse on Vercel production. Speed Insights collecting field data.</done>
  <what-built>
    Phase 3 is fully built and deployed. Speed Insights is installed. The site has:
    - /methodology page with 3 sections (METH-01, METH-02, METH-03)
    - 6 individual program pages at /programs/{slug} with ADVANCE branding (METH-04)
    - Programs overview grid at /programs
    - Desktop nav Programs dropdown + updated mobile nav
    - @vercel/speed-insights collecting field CWV data from production

    This checkpoint gates INFRA-06 (Core Web Vitals pass on Vercel production).
  </what-built>
  <how-to-verify>
    **Step 1: Deploy to Vercel**
    Push to main (or a preview branch). Wait for Vercel deployment to complete.

    **Step 2: Run Lighthouse on key pages**
    Open Chrome DevTools → Lighthouse tab. Run on each of these URLs on the Vercel production/preview domain:
    - Homepage (/)
    - /programs (programs index grid)
    - /programs/advance-engage (individual program page)
    - /methodology
    - /audience

    For each page, check Performance score and Core Web Vitals:
    - LCP: must be < 2.5s (green in Lighthouse)
    - CLS: must be < 0.1 (green in Lighthouse)
    - INP / TBT: TBT < 200ms is the lab proxy for INP

    **Step 3: Functional smoke test**
    - [ ] Visit /methodology — confirm all 3 sections visible with fallback content
    - [ ] Visit /programs — confirm 6 branded program cards in grid
    - [ ] Click "ADVANCE ENGAGE" card → confirm /programs/advance-engage loads with hero
    - [ ] On desktop: hover "Programs" in nav → confirm dropdown appears with all 6 programs
    - [ ] On mobile (resize browser): tap hamburger → confirm Programs section expands with 6 links
    - [ ] On any program page: click the CTA → confirm /contact?program=advance-engage loads with "ADVANCE ENGAGE" pre-selected in the program dropdown
    - [ ] Check InquiryForm dropdown: all 6 programs + "Select a program..." appear

    **Step 4: Check Speed Insights (48 hours after deploy)**
    Visit Vercel Dashboard → your project → Speed Insights tab. Confirm INP, CLS, LCP data is being collected.
  </how-to-verify>
  <resume-signal>Type "approved" if all Core Web Vitals are green and the smoke test passes. Describe any failures (e.g., "LCP 3.1s on /programs/advance-engage — hero image not optimized") if issues are found.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` — succeeds
- `npm run test:e2e` — full suite green
- Human confirms Core Web Vitals pass on Vercel production
- Speed Insights collecting data in Vercel dashboard
</verification>

<success_criteria>
INFRA-06 satisfied: site passes LCP < 2.5s, CLS < 0.1, INP < 200ms on Vercel production across all Phase 3 pages. Speed Insights is collecting real-user field data. Phase 3 is complete and the site is launch-ready.
</success_criteria>

<output>
After completion, create `.planning/phases/03-differentiator-and-launch-ready/03-06-SUMMARY.md`
</output>
